services:
  - type: web
    name: fortunetalk-mcp
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run start:http
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
